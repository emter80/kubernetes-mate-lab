apiVersion: trust.cert-manager.io/v1alpha1
kind: Bundle
metadata:
  name: multipass-root-ca

spec:
  sources:
    - secret:
        name: multipass-root-ca
        #namespace: cert-manager
        key: ca.crt

  target:
    configMap:
      key: ca.crt
