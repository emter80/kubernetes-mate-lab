apiVersion: cert-manager.io/v1
kind: Certificate

metadata:
  name: whoami-cert
  namespace: whoami

spec:
  secretName: whoami-tls

  issuerRef:
    name: multipass-ca
    kind: ClusterIssuer

  dnsNames:
    - whoami.multipass.k3s
