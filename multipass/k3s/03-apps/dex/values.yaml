configSecret:
  create: false
  name: dex-config

service:
  type: ClusterIP

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 256Mi

podAnnotations:
  reloader.stakater.com/auto: "true"
